# 更新紀錄

這份檔案是「最近更新」面板（主畫面右上角）的**唯一資料來源**，由編輯器在建置時直接讀進去渲染，
所以格式是機器要讀的，寫的時候請守住這三條：

1. 日期用 `## YYYY-MM-DD` 一級一天，**新的在上面**；同一天的更新併在同一段裡。
2. 每則更新是一個 `- **標題**：說明` 的項目。冒號用全形 `：`；沒有標題的純句子也收得下，但盡量給標題。
3. 內容**寫給使用者看**：講這個版本之後你能做什麼、哪裡不一樣了，不要寫模組名、函式名、錯誤碼或分支名。
   內部細節請留在 commit 訊息與 `CLAUDE.md`。
4. **一個項目一到兩行**（約 80 字以內）：面板是拿來掃的，不是拿來讀的。次要的細節、取捨與已知限制留在
   commit 訊息裡——寫成第三、第四句之後，連第一句都不會有人讀完。

日期以外的段落（像這段前言）不會被面板顯示，可以安心寫給維護者看。

**寫錯格式的後果是「整天從面板上消失」，而不是報錯**：日期標題的層級（`###`）或格式（`2026/07/30`、`2026-7-30`）
寫錯時，那一天底下的所有項目都會被靜默忽略；用 `*` 而不是 `-` 開頭的項目也一樣。`pnpm check` 有守門測試會擋下
這幾種手誤，但請別依賴它救——照上面三條寫就不會踩到。

## 2026-08-02

- **風格庫看得到 AI 替各簡報決議的設計系統**：新增「AI 產生」區，列出每份簡報自己那套（含第一頁預覽、來源與深／淺標示），按一下就能複製成風格庫項目給其他簡報用。
- **複製出來的是獨立快照**：原簡報維持不變、兩邊之後也不互相同步，複本只帶設計系統、不含參考圖。
- **整份簡報的明暗不再忽黑忽白**：重新分析（或新產生）的風格會鎖定「這份走深色還是淺色」，段落頁可以更深、封面可以滿版影像，但不會有頁面翻到另一邊。既有專案的舊風格要重新分析一次才會鎖住。
- **每頁的構圖交還給 AI**：底色、色票、字型、間距與插圖畫風全份統一，構圖骨架、視覺裝置與插圖內容則鼓勵逐頁不同，不再是一個版型套到底。
- **風格參考圖只借走質感**：分析過的風格不再要求模型照抄參考圖的版面節奏，另一份簡報的主題與 logo 也更不容易跟著跑進來。
- **「AI 自由設計」現在有共用的視覺基準**：產生大綱後會替整份簡報決定一次設計系統，之後每頁都照它畫；沒能決定成功時會在畫面上說明原因與下一步。
- **封面與段落頁不再靠猜**：大綱直接標明每頁是封面、段落頁還是內頁，生成時照著套版面規則；在頁面裡改它會標記為需要重新生成。
- **大綱會刻意換版型**：模型一次看到全部頁面，會避免連續兩頁使用同一種版面結構。

## 2026-08-01

- **簡報檔的備忘稿也讀得到了**：上傳 PPTX 當來源時，講者備忘稿會一起收進來並標明出處，生成大綱時用得上。
- **簡報表格不再夾雜亂碼**：PPTX 表格的第一格以前會黏上一串系統代碼，現在只留真正的內容。
- **內部整理**：兩條文字模型通道的重試與用量統計改用同一份共用邏輯，行為與帳單數字完全不變。
- **版本圖檔整理**：判斷「這張圖還有沒有人在用」的規則收斂成同一份，刪除版本、還原、重繪文字層與匯出的行為完全不變。
- **伺服器內部整理**：後端把所有端點依功能拆成獨立檔案，網址、回應內容與錯誤訊息一律不變。
- **編輯器內部整理**：把編輯畫面拆成多個較小的檔案，往後改一處比較不會牽動其他地方。畫面與操作完全不變。

## 2026-07-31

- **連外抓取更安全**：模型與網頁回應過大時會提早停止；搜尋重導向也會先確認不是內網位址。
- **編輯器規則更一致**：復原／重做、風格參考圖上限與連線欄位驗證都共用同一套規則。
- **Gemini 生的圖變清楚了**：改成向模型要兩倍解析度再縮到投影片尺寸，細字與細線不再糊，每頁只多等約兩成時間。
- **移除本機 Codex 通道**：影像、文字與搜尋一律走 OpenAI 相容端點或 Gemini 原生。既有設定會自動轉換，你自訂的連線與組合都會保留。
- **一行指令裝好整套**：新增 Docker Compose，同時起 Slide Maker 與 CLIProxyAPI；登入一次 Codex，生圖與大綱就走你的訂閱額度，不必再申請按張計費的 API key。
- **安裝說明改寫**：README 現在把 Docker 與從原始碼裝並列成兩條路，附上該選哪一條的對照表與 Docker 那三行指令。
- **自動搜尋開關改成跟著專案走**：不再是各自瀏覽器的偏好，同專案的所有人共用一份，不會再互相覆寫。開啟設定時會取一次最新值，但開著的期間不會即時反映別人的變更。
- **搜尋設定收成一個勾選框**：系統設定裡的三選一（Live／Cached／Disabled）其實只有開和關的差別，改成與上傳素材那一步同一個「自動搜尋網路資源」。

## 2026-07-30

- **可以只下載當前這一頁的 PNG**：不用再為了一張圖下載整包 zip；檔名帶頁序，內容與整包裡的同一頁一模一樣。
- **匯出面板收成兩區**：四個整份下載歸「專案」，單頁下載放「當前頁面」，一眼分得出哪個是整份、哪個只有一頁。
- **主畫面新增「最近更新」**：右上角可以打開更新紀錄，依日期看到每天做了什麼。
- **編輯器可以只用鍵盤操作**：投影片縮圖按 Enter 或空白鍵就能選頁。
- **文字可以加描邊了**：白字壓在照片或漸層上看不清楚時，勾起來就讀得到；粗細隨字級等比縮放，畫布與四種匯出都會帶上。
- **文字框的屬性面板變短了**：底色與描邊收成同一行的兩個勾選框，細項進下拉，字體、字級、顏色不必再往下捲。
- **縮圖上的按鈕精簡成三顆**：拿掉上移／下移（改用拖曳排序），隱藏鈕換成眼睛圖示，睜著代表這頁會上場。
- **彈出視窗的行為補齊**：Esc 都關得掉（動作進行中除外），關閉後焦點回到原按鈕，背後的換頁與刪除也不會再被誤觸。
- **要等很久的按鈕按下去會立刻有反應**：重建大綱、生成、複製／刪除頁面等按下即鎖住，連按兩下不會再跑兩輪或建出兩份副本。
- **生成失敗可以就地重試**：失敗訊息旁邊多了「重試」，不用再繞回去重新點一次。
- **大綱改過但還沒重新生成時會講出來**：直接寫出是哪幾欄與圖片不同步，不會等到匯出才發現圖還是舊的。
- **模型庫刪除前會先問**：會告訴你有幾個模型／組合正在用它；欄位沒填完的錯誤也改成顯示在該欄位旁邊。
- **風格編輯器的狀態不再互相混淆**：上傳參考圖時「儲存」不會再假裝在儲存；載入失敗會給重試與返回，不留空白表單讓你覆蓋掉原風格。
- **小字與淡色文字看得清楚了**：說明文字放大到看得舒服的大小，淡灰文字與輸入框邊框的對比度拉到無障礙標準。
- **小按鈕變好按**：刪除、隱藏、關閉這些圖示鈕放大到不容易點歪的尺寸，卡片與縮圖的 hover 也不再瞬間跳色。
- **抽離文字進行中，畫面不會再一起卡住**：背景工作跑起來時，圖片載入、調整頁序與重新生成大綱都不會再被擠掉。

## 2026-07-29

- **抽離文字會把誤讀的簡體字改回繁體**：只動真正的簡體字，「台積電」「貪污」「祕書」不會被改成別的寫法；可在對話框關掉。
- **文字樣式沒抓到會直接告訴你**：估不出原本的字色與字型時會明講這頁是預設樣式（白字 Arial），不再安靜給你一份走樣的文字層。
- **大綱改成兩階段生成**：先訂骨架再展開內容，長簡報的結構更穩；每個專案的資料來源上限也從 100 份放寬到 200 份。

## 2026-07-28

- **側邊欄可以收起來**：右側面板加了收合鈕，需要專心看畫布時可以收掉，展開後未存檔的大綱草稿仍在。
- **一次抽離全部頁面的文字**：專案分頁新增批次抽離，不用一頁一頁點。
- **抽離文字改成排隊執行**：同時只跑一件，連續丟很多頁不會互相卡住或整批失敗。
- **頁首不再被長專案名擠爆**：很長的專案名不會再把上方導覽與狀態列壓成兩行；來源數量移到分頁標籤上。

## 2026-07-27

- **文字工具列會自己換方向**：畫布寬度不夠時，工具列改成橫排放到畫布下方，不再擋住投影片。

## 2026-07-26

- **頁面可以隱藏**：隱藏的頁面放映會跳過、不進 PPTX／PDF、也不佔頁碼，但仍可編輯與生成，PNG 與專案封存照樣完整保留。
- **簡報模式修正**：退出放映會停在剛剛放到的那一頁；頁數計數會反映隱藏頁；很小的文字框也能雙擊進入編輯。

## 2026-07-25

- **沒抽離過的圖也能直接加文字**：不必先跑抽離文字，就能在任何一頁疊上可編輯的文字框。
- **抽離文字新增「文字修復」選項**：預設不修復，需要時可以用大綱內容校正認錯的字。
- **大綱太長時會逐頁收斂**：只調整真正超標的頁面，不再整份重來；真的做不到時會給一句知道下一步的中文說明。
- **上傳的圖片會自動產生可搜尋的描述**：背景讀出圖裡的文字與數值，之後才搜尋得到、進得了大綱；要不要讓 AI 讀，在上傳按鈕旁自己決定。
- **畫布優先的版面**：編輯器版面重新調整，文字工具列改成圖示化。
- **匯入入口收進「最近簡報」標題列**，專案封存改用 `.slide-project.zip`。
- **放映控制列縮到左下角**，沒有動滑鼠時會淡出。

## 2026-07-24

- **資料來源可以直接貼網址**：一次最多 10 筆，抓不到正文的會逐筆說明原因與下一步；動態網頁可另外啟用補抓服務（等於把網址送給第三方）。
- **頁碼交給系統畫**：改由編輯器統一合成，可設起始頁與跳過第一頁，不再讓 AI 畫在圖上；PDF 匯出改內嵌 JPEG，檔案小很多。
- **抹字大幅改善**：抹掉原文字時不再咬斷圖表線條、不再把有顆粒的背景整片抹平，被文字壓住的表格線與座標軸也會接回來。
- **局部修圖真的會照你框的範圍改**：修正了遮罩傳給模型形同隱形的問題。
- **投影片上不會再出現 `**` 之類的符號**：圖片生成改送結構化內容，markdown 記號不會被畫進圖裡。
- **文字框快捷鍵**、簡報模式可以滾輪換頁、文字框可設底色。
- **建立流程第 3 步加入自動搜尋開關**：關掉時會要求你自己提供素材。
- **複製頁面會一併帶上圖片的版本歷史**。

## 2026-07-23

- **抽離文字改用本地抹字引擎**（並設為預設）：不必呼叫影像模型就能把原本的文字抹掉，比較快也不消耗配額。
- **歷史版本可以刪除**：刪掉不要的版本並回收檔案，不會動到目前使用中的版本。
- **大份簡報匯出不再失敗**：匯出改成串流傳輸，繞開雲端的回應大小上限。
- **雲端環境補上中文字型**，抽離文字在雲端的失敗一併修掉。

## 2026-07-22

- **抽離出來的文字位置更準**：文字框的位置與字級改用實測的字型度量對位，貼回圖上更貼合原稿。
- **支援 Gemini（AI Studio）原生端點**：出圖、文字與網路搜尋三種能力都能用；設定不一致會在存檔時就擋下，不會等到執行才出錯。
- **圖片編輯進入後直接就能框選**，不用再切換框選開關。
- **PDF 匯入修正**：重複文字、逾時預算與「其實成功卻報失敗」的頁面。

## 2026-07-21

- **看得出來源是你指定的還是 AI 挑的**。
- **表格會保留結構**：從 Office 檔與 PDF 抽出的表格不再被壓成一串文字，生成時也會要求忠實呈現。
- **新增頁面改成建立空白頁**：移除了會偷偷上網搜尋的單頁生成。
- **風格參考圖分析改為產出結構化的設計系統**（顏色、字級、間距等）。

## 2026-07-20

- **來源面板加入全文搜尋**：來源多的時候可以直接搜內文。
- **模型庫介面調整**。
- **修正生圖時的矛盾指令與參考圖內容外洩**。

## 2026-07-19

- **建立流程新增「上傳素材」步驟**：一開始就能把手上的檔案帶進來。
- **新增模型庫**：可以自訂連線、模型與組合，指定文字、影像、搜尋各要用哪一個。
- **風格編輯器可以從 PDF 建立參考圖**。
- **支援 OpenAI 相容端點**：影像、文字、搜尋三種能力都能接。
- **網路來源改為「抓得到正文才算數」**：模型自己講的摘要不會被當成已驗證來源，私有網路位址也擋掉了。
- **PPTX 匯出與畫布上的文字排版修正**。

## 2026-07-17

- **第一版**：以圖片為主體、文字可直接編輯的簡報編輯器。
