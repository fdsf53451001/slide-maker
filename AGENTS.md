# Shared agent instructions

Edit this file; it is the single source of truth synced to every CLI.

## 指令

- 安裝依賴：`npx pnpm@10.13.1 install`（pnpm 版本固定為 10.13.1）
- **完整驗證閘門（完成程式碼變更後必跑）**：`pnpm check`（= `pnpm typecheck && pnpm test && pnpm build`）
- 單一套件測試：`pnpm --filter @slide-maker/server test`；單一測試檔：`pnpm --filter @slide-maker/server exec vitest run test/config.test.ts`
- 開發伺服器：`pnpm dev`（長時間執行；先 build 全部套件再啟動 server，網址 http://localhost:4173）；純 UI 開發用 `pnpm dev:web`（Vite）
- 格式化：`pnpm format`（Prettier）

## 注意事項

- 開發模式依賴 `NODE_OPTIONS=--conditions=development` 搭配 tsconfig 的 `customConditions: ["development"]`，讓 workspace 套件解析到 `src/*.ts` 原始碼；少了這個條件會解析到 `dist/` 舊建置產物。
- TypeScript 開啟 `noUncheckedIndexedAccess` 與 `exactOptionalPropertyTypes`——索引存取結果須處理 `undefined`，optional property 不可顯式指定 `undefined`。
- **給模型的重試指令必須有受詞**：叫模型「改掉上一輪那個東西」時，那個東西一定要真的在這一次的 prompt 裡。`runStructured` 是**單次無狀態呼叫**（每次重建 `[system, user]`，沒有對話歷史），所以「你上次寫的那份」「與上次相同」這種措辭在模型眼中不存在，它只能從同樣的輸入重寫一遍、落在同樣的結果——重試迴圈於是結構性地不收斂，看起來卻像模型不聽話。`OUTLINE_CONTENT_TOO_LONG` 連續發生正是這個：重試指令要模型「從上一輪草稿砍掉 N 單位」，但 payload 裡只有使用者的原稿，那份超標草稿從來沒進過 prompt；連續三輪修 prompt 措辭都沒用，把草稿餵回去才收斂（`app.ts` 兩條大綱路徑的 `previousAttempt`）。同理，逐項的數字要**逐項**給：整份大綱用單一「最長頁超額」對每一頁下砍字令，只超 5 單位的頁會被要求砍 100，那正是 `outlineDataFidelityInstruction()` 要防的過度刪減。
- **失敗要在伺服器留下可判讀的原因，不能只往上拋碼**：重試耗盡、降級採用次佳結果、吞掉例外沿用原結果——這幾種路徑都必須 `logWarn`／`logError` 記下**為什麼**（實測值、上限、第幾輪、哪幾筆），否則使用者回報「連著三次失敗」時，伺服器端一行證據都沒有，只能靠猜。`logWarn("outline_content_overflow", …)`、`renderedBy`／`metadata.failureReason`、`metadata.imageDescriptionFailure` 都是這條的實例。**只記 id 與數字，正文（content、prompt、來源內文、憑證）一字不進 log**，並用測試釘住（`outline-overflow.test.ts` 直接斷言 log 序列化後不含正文字串）。
- **可選步驟的失敗要先分「設定錯誤」與「執行期失敗」，兩者處置不同**：設定錯誤（`resolveStructuredText` 丟 `ModelLibraryError`——`COMBINATION_NOT_FOUND`／`COMBINATION_TEXT_MISSING`／`NO_DEFAULT_COMBINATION`／`TEXT_MODEL_NOT_FOUND`）使用者現在就修得好、而且必然整份產出都缺那一塊，所以要在**花掉配額與時間之前**擋下（4xx＋錯誤碼＋能導向下一步的繁中 message，代碼沿用模型庫那幾個好讓前端分辨，訊息換成該端點自己的——通用的「找不到模型組合：\<id\>」沒有下一步）；執行期失敗（availability 不是 `available`、`runStructured`／schema parse 丟錯、模型回了但一個 id 都對不上）當下修不好，擋了只是把已經跑完的工作丟掉，所以降級繼續，但**必須留代碼並把結果回報前端**。抽字的視覺樣式精修是這條的實例：`app.ts` 的 extract-text 在 OCR **之前**（也在 `readiness.assertCanGenerate()` 之前，那可能真的打一次 preflight）解析文字 provider，失敗回 409＋`text_extraction_style_model_unresolved`，測試以「`ocr.recognize()` 一次都沒被呼叫」釘住；執行期失敗才降級並記 `ocr_style_refine_skipped`／`ocr_style_refine_failed`／`ocr_style_refine_empty`／`ocr_style_refine_partial`，結果隨 202 的 `textExtraction.styleRefinement:{applied,reason,detail}` 回前端（`generationJobSchema`，optional＝舊 job 視同有套上；`detail` 只帶 provider 的 `availability.reason`，那是靜態設定字串、往往正好是下一步），前端於 `applied:false` 時用既有的非錯誤通知列講明「這一頁的字色與字型是預設值（白字 Arial），不是從圖上估出來的」並在下一次抽字時清掉它（通知列只有點擊才關得掉，成功路徑刻意不寫訊息，不清就會指著一份其實有風格的產物），批次則在摘要裡列出是哪幾頁；設定錯誤碼一併進 `OCR_BATCH_ABORT_CODES`（下一頁不會變好），且**不可**算進「撞出中止的那一頁已處理」——它擋在 OCR 之前，那一頁一點事都沒發生，算掉會讓「還有 N 頁沒有送出」少一頁。實機證據：專案綁到已被刪除的組合 → 空 catch 靜默略過 → 整頁 31 個框全是 `#ffffff` ＋ Arial（`boxesFromOcr` 的預設值），伺服器零證據，只能靠與舊版本（`#111111`×22／`sans-serif`）對照反推。抽字是開新版本、跑抹字、燒 OCR 與可能的影像模型配額的**破壞性**操作，做出一份沒有風格的文字層等於整趟白做，而使用者無從察覺。反例（**不要**跟著改）：圖片描述的 `imageDescriptionProvider` 仍是靜默降級——沒有描述的圖片來源照樣可用（只是 FTS 撈不到），讓上傳因為組合沒設好就整批失敗是更糟的交換；但「安靜略過」不等於「不留證據」，它現在也記一行 `image_description_model_unresolved`。
- **「模型沒有 throw」不等於「它做了事」，可選步驟一定要有命中計數**：`ocrStyleRefinementSchema` 對 `boxes` 只有 `.max()`、沒有下限也不比對 id，而 `applyStyleRefinement()` 是 `styles.get(box.id)`、查不到就原樣放行——所以 `{"boxes": []}` 與「模型自己編一組 id」都會 parse 成功、不 throw，靜默產出一份整頁白字 Arial 的文字層，**與樣式精修整段沒跑長得一模一樣，只是換了個入口**。命中數由 `applyStyleRefinement` 自己回傳（`matched`），不可讓呼叫端另外數一次；`matched === 0` 當降級（`STYLE_REFINE_EMPTY`），`0 < matched < boxCount` 不算降級但要留一行帶兩個數字的 log。CLAUDE.md 另一條已寫明非嚴格 gateway（尤其 Gemini 系）不遵守 `json_schema`，這正是它們常見的失敗形狀，而且比 zod 直接爆掉更難察覺。
- **模型解析的 registry miss 要轉成具名 `ModelLibraryError`，不能讓裸 `Error` 變成 500**：`ProviderRegistry.get()` 對查不到的 id 丟的是裸 `Error("Unknown provider: …")`，錯誤中介層只能把它落到 500 `INTERNAL_SERVER_ERROR`（沒有 message、沒有下一步），但這其實是使用者改得掉的設定問題——`ModelRuntime#buildText` 對 `providerKind` 是 `mock`／`local` 的 entry 回 undefined（不註冊），而模型庫 UI 的種類清單含 `mock`、組合的文字下拉又只濾掉 `local`，所以「能力＝文字、種類＝mock」的 entry 綁進組合就踩得到（模型被刪但組合還引用著也是同一條）。`resolveTextProvider` 因此把它包成 `TEXT_MODEL_NOT_FOUND`；抽字與兩條大綱路徑一起受惠。**`resolveSearchProvider` 至今仍是同一個形狀的裸 500，尚未處理。**
- **`logWarn` 的第三個參數會把 `error.message` 與 `stack` 整份序列化進 log，例外本身可能就夾帶正文**：非嚴格 gateway 會把 request body 原樣回聲進 400 的 message（那份 body 含 `OCR_BOXES_JSON` 與每一框的正文），zod 的 `invalid_enum_value` 也會把收到的值寫進 `ZodError.message`。所以罩住「provider 呼叫＋schema parse」的 catch 不可直接 `logWarn(event, fields, error)`，要先過濾（`app.ts` 的 `styleRefineErrorFields()`：只留型別名、`SafeProviderError.code`、zod 的欄位路徑）。釘這件事的測試**必須用真的會洩漏的例外形狀**（讓 stub 丟 `` `HTTP 400 from gateway: ${request.prompt}` ``、或回一個 enum 欄位塞了正文的 payload），拿 `new Error("HTTP 503")` 這種不含正文的訊息去測，改動前的程式碼也會通過＝等於沒測。
- 伺服器環境變數皆定義於 `apps/server/src/config.ts`（如 `SLIDE_MAKER_DATA_ROOT`、`SLIDE_MAKER_MODEL_TIMEOUT_MS`），非法值會在啟動時 throw。**本機 Codex CLI 通道（`packages/provider-codex`）已於 2026-07-31 移除**：實測它與 cli2proxy 走的是同一個 image tool、拿到的原圖同為 1672×941，畫質沒有優勢，卻慢 2.8–6.8 倍且完全不回報 token 用量；`providerKind: "codex"` 這個值也從 schema 拿掉，舊 `models.json` 由 `apps/server/src/model-library-migration.ts` 在 parse **之前**清洗（詳見該檔的註解）。
- OpenAI-compatible 端點（CLIProxyAPI、OpenAI 官方、LiteLLM 等皆為不同 base URL）由 `packages/provider-openai` 支援，涵蓋三個能力：影像生成（`openai-image` provider）、結構化文字生成、網路搜尋。以環境變數設定：`SLIDE_MAKER_OPENAI_BASE_URL`（http/https，如 `http://localhost:8317/v1`）、`SLIDE_MAKER_OPENAI_API_KEY`、`SLIDE_MAKER_OPENAI_IMAGE_MODEL`、`SLIDE_MAKER_OPENAI_TEXT_MODEL`、`SLIDE_MAKER_OPENAI_SEARCH_MODEL`（可選，預設同 text）、`SLIDE_MAKER_OPENAI_TIMEOUT_MS`。網路搜尋已從文字推理解耦（先由 `WebSearchProvider` 取得來源，再餵給純推理的 `StructuredTextProvider`）；CLIProxyAPI 的 Gemini Chat translator 必須收到 `tools: [{ google_search: {} }]` 才會轉成原生 `googleSearch`，不可送 OpenAI typed `web_search`。搜尋候選必須成功抓取網頁正文才可進入生成 prompt，模型產生的搜尋摘要不得作為已驗證來源。三條圖片通道共用 `packages/core/src/image-contract.ts` 的合約，transport adapter 不得另立內容／風格／reference 規則：`SLIDE_MAKER_OPENAI_IMAGE_API=chat`（`/chat/completions`，CLIProxyAPI 的 GPT tool image 或原生 `gemini-3.1-flash-image`，可帶有序參考圖）；`SLIDE_MAKER_OPENAI_IMAGE_API=images`（預設，`/images/generations`＋`/images/edits`，如 `gpt-image-2`，參考圖生成透過 `/images/edits` 的 `image[]` 陣列，edit base/mask 亦走此端點）。三者輸出皆正規化成 canvas 尺寸 PNG。**遮罩一律以「攤平成不透明黑底白框的可讀影像」放進 `image[]`，不送官方的 `mask` 欄位**（這個決定違反直覺，不要「簡化」回去）：官方 alpha 語意與本專案相反（官方透明＝可編輯，本專案白＝要改），送未翻轉的遮罩對嚴格端點等於「保留使用者選的區域、改掉其他全部」，而攤平後的圖不透明、本來就不能同時當 `mask` 欄位用；合約的 `Image N is a locator … Read it to find that part` 也只有攤平版讀得到。**這裡曾寫「模型讀不到 `mask` 欄位」，那是錯的**：CLIProxyAPI 確實會把 multipart 的 `mask` 轉成上游的 `input_image_mask.image_url`（`codex_openai_images.go:773`），2026-07-31 以「只把遮罩區填成洋紅」的可判定 prompt 實測 8 次，命中框內 `100 / 99.8 / 84.3 / 25.2 / 14.3 / 12.2 / 7.5 / 7.4`（%），對照組（不送）5.9%——**3/8 精準、5/8 等同沒送**，所以它是不可靠而非讀不到，結論（空間保證不能靠它）不變。同批實測另外兩件事：`input_fidelity=high` 是反效果（單獨用 22.1%，與 mask 併送把 100% 拉回 22.4%），**不要送**；遮罩位置本身是有被理解的（同圖同指令只換遮罩位置，改動會跟著跑，另一個位置恆為 0.0%），失準的是**邊界**，而且與內容有關（純背景區三條通道都命中 89.7–99.7%，靠畫面邊角又有既有物件時 gpt-image-2 掉到 21.8%，只有 `gemini-3.1-flash-image` 兩處都 96%+）。空間保證由 `jobs.ts` 的 `compositeMaskedEdit` 承擔——它逐位元驗證過：遮罩外 **0 px** 改動，前提是傳給它的遮罩是**帶 alpha 的原始版**（`dest-in` 吃 alpha），餵攤平版會整張放行。`image[]` 必須依 index 順序 attach `request.references` 的**每一筆**（含補充風格／內容參考圖），否則合約列出的 `Image N` 會與實際附圖錯位。使用者上傳的遮罩在 `app.ts` 存檔時就以 `kernel:"nearest"` 正規化到 canvas 尺寸（二值遮罩放大走 bicubic 會 overshoot 出半透明過渡帶，`dest-in` 後變成邊界鬼影）。`gemini-3-flash-agent` 不視為可用圖片輸出模型。非嚴格 gateway（尤其 Gemini 系）不遵守 `json_schema`，故文字/搜尋 provider 會加 JSON-only system 指令並寬鬆解析（去 ` ```json ` 圍欄）。
- Gemini 官方 API 走 `packages/provider-gemini` 的原生 `:generateContent`（AI Studio，base URL 形如 `https://generativelanguage.googleapis.com/v1beta`，認證用 `x-goog-api-key` header），不走 Google 的 OpenAI compat 層——實測該 compat 層無法序列化影像輸出（400 `Unhandled generated data mime type: image/jpeg`）、沒有 `/images/edits`、也拒絕所有 Google Search grounding 語法。三能力都在原生端點上可用：影像 `generationConfig.responseModalities:["IMAGE"]`＋依序 `inlineData` 參考圖（**Gemini 沒有獨立 edit/mask 端點，遮罩就是「再多一張圖」**，語意仍全由 `image-contract.ts` 承擔），回傳影像的 mimeType **因模型而異**（2026-07-22 實測 `gemini-2.5-flash-image` 回 `image/png`，其餘三個影像模型回 `image/jpeg`），一律讀 `inlineData.mimeType` 再交給 `rasterToCanvasPng()` 正規化成 canvas PNG；**`generationConfig.imageConfig.imageSize` 固定送 `"2K"`，這是畫質關鍵而非可選調校**——2026-07-31 實測不送時模型只回 1376×768，正規化得**放大 1.40×**，送 `2K` 回 2752×1536 變成**下採樣 0.70×**；端到端跑真實投影片 fixture 的銳利度（Laplacian 變異數）**99.3 → 430.1**、0.87–1.0 頻段高頻能量 0.0042% → 0.0300%，代價只有耗時 +17%、token +16%（3296 → 3819）。`4K` 回 5504×3072／9 MB 對 1920×1080 畫布是浪費。**影像的量測一律要用有內容的 fixture**：同一組改動拿純色測試圖估出來的是「34.8 → 66.9、成本 +60%／+36%」，收益低估五倍、成本高估三倍，兩邊都會導向錯的決定（先前「gpt-image-2 只出 3:2、要裁掉 15.6%」的誤判也是同一個成因）。`imageSize` 與 `aspectRatio` **分開判斷**（解析度與比例無關，非 16:9 時仍要送 `imageSize`）。同一個決定在 CLIProxyAPI 的 chat 通道也要做，那邊走 translator 認得的 OpenRouter 風格頂層 `image_config:{image_size,aspect_ratio}`（`packages/provider-openai/src/image-chat.ts` 的 `geminiImageConfig()`），**只對 `/^gemini-/i` 的模型送**（比照 `web-search.ts` 的 `searchTool()`，其他路由沒有對應翻譯、嚴格端點可能拒絕未知欄位）；文字只送 `responseMimeType:"application/json"`，**不可送 `responseSchema`**（只吃 OpenAPI subset，塞完整 JSON Schema 會 400）；搜尋送 `tools:[{googleSearch:{}}]`（不可與 `responseMimeType` 並用），來源直接讀 `groundingMetadata` 而非要模型輸出 JSON。回應 part 可能夾帶 `thoughtSignature`，解析時只認 `text`／`inlineData` 鍵。grounding 的 `web` 只有 uri 與網域名 title、**沒有 summary**，summary 由 `groundingSupports` 的 segment 反向聚合，組不出來的 chunk 一律捨棄；uri 是有時效的 302 中繼網址，provider 內會先解成真實網址（安全檢查共用 `packages/core/src/url-safety.ts`，與 `web-capture.ts` 同一份）。連線層的 `protocol` 欄位（`openai`｜`gemini`，預設 `openai`）決定「列出可用模型」走 `GET /models` 還是 `ListModels`；model entry 的 `providerKind` 必須與所引用連線的 `protocol` 一致，不符時 server 於寫入時丟 `CONNECTION_PROTOCOL_MISMATCH`（否則執行期只會看到難懂的 HTTP 404）。grounding 的 `groundingChunkIndices` 是**多對一**語意（一段輸出可同時被多個 chunk 支撐），只有專屬單一 chunk 的段落才算該頁摘要，多重支撐段落降級為加註前綴的補充；重導向解析對整批候選併發進行並套用共用預算，解析後的網址會去重與再驗一次安全性（起點 uri 就不合法者整筆捨棄，不可退回原 uri——下游 `captureWebPage` 對這種網址是直接 throw）。
- 本地抹字（`scripts/local_inpaint.py`，`local-inpaint` provider 服務 extract-text）判背景**不是**「ROI 灰階眾數 ± 門檻」，而是三道疊加的檢查，缺一都有實測失效：①**顏色蔓延**——從遮罩框外取種子做容差 flood，走得進框內的顏色是背景（底色、漸層、紋理、軸線、格線、卡片邊框、色塊在框外都有本體，文字則被 `textMask` 的 padding 包住、框外沒有延續）；②**顏色歸屬**——走進來的顏色還必須出現在框外樣本裡（32³ colour cube），少了這道，重新編碼／縮放過的柔邊文字會沿低梯度爬進字身，實測 σ=1.6 模糊的灰字殘留 3637 px；③**貫穿檢查**——背景裡「不貫穿 ROI 外緣」的非平坦色團塊，是遮罩切在筆劃中間時字沿自身洩漏進來的，改判回墨（少了這道，整行相連的墨只要露出 1px 就完全不被抹）。接著三條不變量不可退回：膨脹後的字墨一律 `&= region`（**遮罩外零改動**，舊版 7×7 膨脹 2 次外擴 6px 正是圖表線被咬斷的根因）；穿過遮罩的結構原樣保留；被文字蓋住的水平／垂直線由「抹除帶兩端同色則線性插值」接回——兩軸都可橋接時比的是「插值色離**框外平坦底色**多遠」，不可拿 inpaint 結果當參考（Telea 在帶子邊緣已擴散出線色，會讓判斷反過來），也不可固定偏好某一軸（垂直的表格欄線、Y 軸、「｜」分隔線會整段被底色蓋掉）；**沒有對手的插值一律要自證承載結構**——另一軸被鄰近抹除帶佔住探針、或這是超過 `BRIDGE_MAX_GAP` 的寬帶時，若放它無條件覆蓋，它會拿垂直方向的底色把整條線塗白（實測 400px 的線 100% 被塗掉）。`BRIDGE_MAX_GAP` 因此是「需自證」而非硬拒：整行標題壓在表格線上輕易就超過它。已知限制：墨若與貫穿結構相連（文字壓在同色線上）就分不開，會保線並殘下少量字；完全不延伸出遮罩的元素（自身 padding 比 `textMask` 的 `padX`/`padY` 還小的膠囊、圖例色塊）沒有框外本體，會被當墨抹掉。`FLOOD_TOLERANCE=6` 只是**平坦底色的下限**：顆粒／照片背景的相鄰像素差本來就更大，固定容差會讓整片背景蔓延不進去而被當墨抹平（σ=16 顆粒實測損壞 38% 乾淨區域），所以容差、以及第③道的 `LEAK_DELTA`，都改成依「框外背景相鄰差的中位數」逐遮罩自適應（平坦底色估出來就是下限，框外少數線與邊緣拉不動中位數）。柔邊低對比字**要靠第②道解，調大容差只會更糟**；JPEG q95／q88／q75 往返後仍成立，但那是壓縮雜訊、不等於柔邊。上述行為由 `apps/server/test/local-inpaint-pixels.test.ts` 逐像素釘住（需 `.venv-ocr`，未安裝時整組 skip，CI 不跑）。
- extract-text 的簡→繁（`apps/server/src/traditionalize.ts`，端點旗標 `traditionalize`，**預設開**）是「詞級轉換、但只採納簡體專屬字的位置」，**不是**整段套 OpenCC——抽出來的字會被重繪回投影片圖上，把原本正確的繁體改成另一個字形是使用者難以察覺的可見錯誤，比漏修嚴重得多（實測 `台積電`→`臺積電`、`台北101`→`臺北101`、`一台機器`→`一臺機器`、`鄰里`→`鄰裡`）。詞級仍是必要的：`头发`→`頭髮` 對上 `发展`→`發展` 的一簡對多繁消歧只有帶上下文才命中，逐字轉 `发` 固定給 `發`。因此模組裡有**兩條鏈，不可合併**：產出用 `ConverterFactory(from.cn, to.tw)`（簡→繁＋台灣字形），而判定「這個位置該不該換」的閘門**只掛 `from.cn`**——TWVariants 的左欄全部是繁體正字（`祕 秘`、`喫 吃`、`峯 峰`、`污 汙`、`癡 痴`、`覈 核`、`脣 唇`、`爲 為`、`裏 裡`、`麪 麵`… 共 38 條），閘門若問「完整 s2tw 鏈有沒有改變這個單字」，這 38 個原本就對的字會全被判成可轉，實測 `貪污`→`貪汙`、`祕書處`→`秘書處`、`純喫茶`→`純吃茶`、`王建峯`→`王建峰`。異體正規化（`裏`→`裡`、`麪`→`麵`、`爲`→`為`）即使看起來像改善也一律不做，因為同一批裡的 `祕`→`秘`、`喫`→`吃` 是改壞的，行為要可預測。唯一的例外 `着`（只在 TWVariants、簡體常用、台灣不用這個字形）以**顯式一字白名單**放行——白名單看一眼就知道總共放行了誰，讓整條鏈漏過來則不可稽核。豁免清單 172 字由上游 `BYVoid/OpenCC` 的 `data/dictionary/STCharacters.txt` 推導（判準是「該簡體字形的繁體候選列表包含它自己」＝這字形在繁體中本來就是正字），opencc-js 只留第一候選、repo 內重現不了；另有 `坏`／`么`／`无` 三個機械判準漏掉的人工補充，兩份刻意分開列，免得下一個人以為重跑推導生得出它們。**必須排在 `refineOcrBoxes` 之前**：`textRepair:"outline"` 拿繁體大綱做模糊比對，先修復再轉繁等於用混著簡體的字串去比，編輯距離被拉過 `MAX_ERROR_RATIO`（34%）就整行對不上。刻意接受的漏修：`干杯`／`后台`／`里程碑`／`制造`／`云计算`（字形在繁體中合法，`云计算` 只會變成 `云計算`），以及沒有假名的純漢字日文（`株式会社`、`国際会議`）——含平假名／片假名的框整框跳過（假名是零誤判訊號；日文新字体被半轉換成 `體験版のご案內`、`社會保険` 比原文更難讀）。
- 上傳成 `visual-reference` 的圖片會在**背景**跑一次 vision 抽取（`apps/server/src/image-description.ts`），把圖裡的軸標籤、數值、表格與文字寫進 `extractedText`／`chunks`——沒有這一步，圖片沒有 chunk、FTS 撈不到、進不了大綱 prompt，模型也就沒有理由把它放進 `slide.sourceIds`，`jobs.ts` 那條「選中的視覺參考附成參考圖」的路等於永遠掛不上。以 `SLIDE_MAKER_IMAGE_DESCRIPTION`（`on` 預設｜`off`）整條開關。**其他 usage 一律不跑**（`style-reference` 由風格分析負責、`direct-asset` 是原樣素材、`exclude-from-generation` 不參與生成）；改用途成 `visual-reference` 時只有前端**明講會消耗配額並取得同意**後帶 `describeImage:true` 才補跑。
- 圖片描述的**授權閘門**（`allowModelAccess`）是這條路唯一的硬條件，有三個地方缺一不可：①上傳端點的 query 解析**不可用 `z.coerce.boolean()`**（`Boolean("false") === true`，等於永遠關不掉，實測 `false`／`0` 全被吃掉）；②排隊十幾秒後真正要送出前必須拿**最新**狀態重跑 `shouldDescribeImageSource()`（使用者會在排隊期間取消勾選）；③寫回的 `updateProject` callback 內再檢一次才落地（in-flight 那段擋不住送出，但擋得住進大綱 prompt）。**前端已經沒有事前攔截點**：上傳一律送 `allowModelAccess=true`，上傳按鈕旁那個勾選框於 2026-08-04 依產品決定移除（「會上傳＝要準備使用」），代價是圖片一落地就會被排進讀圖、沒有任何辦法在送出**之前**擋下來，使用者唯一的撤銷手段是來源卡片上那顆事後 toggle——它擋得住之後進大綱 prompt，擋不住已經送出去的那一次。因此②不是變得不重要而是**變得更重要**：它原本防的是「使用者在排隊期間取消勾選」，現在那個情境改由卡片 toggle 觸發，而卡片 toggle 是使用者反悔時唯一按得到的東西（排隊十幾秒剛好是他看到縮圖、發現這張不該給 AI 讀的那段時間）。前端一律送 `"true"` 這件事由 `ImageSourceDescription.test.tsx` 斷言 query 字面釘住：改回 `false` 會讓整條路靜默失效（伺服器是安靜跳過，不報錯）。
- 圖片描述的其餘不變量：上傳端點不等描述，先落地回 201（狀態標 `parsing`，沿用既有 enum），背景寫回後前端靠既有的專案輪詢自己更新，卡太久就停手並改口「可能已中斷」（否則卡住的來源會讓每個分頁永遠每 1.5 秒打一次）——判斷用的是**客戶端錨點**（第一次看到它是 parsing 的時刻）而非伺服器時間戳（客戶端時鐘快幾分鐘就會讓每張新圖在 t=0 被判逾時），預算也要照併發與單張上限隨批次張數放大（`parsingBudgetMs`；固定 5 分鐘在七張以上的批次就會誤殺）；同時最多 2 個（前端是並行上傳多檔，十張圖同時打十個 vision 請求必撞限流）。單張有 `IMAGE_DESCRIPTION_TIMEOUT_MS` 的**自有 abort 上限**——`StructuredTextRequest.timeoutMs` 現在**沒有任何 provider 會讀**（openai／gemini 都忽略它；唯一讀它的 codex 通道已移除），靠它等於沒有上限。縮圖 `.rotate()` 一定要在 `.resize()` 之前（手機照片靠 EXIF orientation，瀏覽器會套用、sharp 不會，少了它使用者看到正的、模型讀到躺的），編碼跟著來源走（PNG→PNG 保住細字，JPEG→JPEG 省下 10–20 倍 request size）。產物是**模型衍生物**：`extractedText` 開頭與**每一塊** chunk 都帶標註（chunk 是被單獨切出來餵進 prompt 的，前綴長度要從切塊視窗裡扣掉，否則超出 `source-context.ts` 的 1600 截斷），`metadata` 記下產生它的模型並顯示在來源詳情。**前端不得鏡射伺服器組態**（開關、有沒有文字模型、模型可不可用）來預測會不會讀圖——那必然漂移；改用途的確認框問完之後看回應裡這筆來源有沒有真的變成 `parsing`，沒有就直說「沒有呼叫模型」。任一步失敗都只留 log、把來源放回 `indexed`＋空 `extractedText`，並在 `metadata.imageDescriptionFailure` 記下原因供前端說明（最常見是選到的文字模型不支援讀圖）——絕不影響上傳。`parsing` 是執行期狀態，**不得跨程序旅行**：啟動時、`slide-project` 匯出與匯入時都要正規化回 `indexed`，否則前端永遠顯示「分析中」。
- PDF 有**三條互不共用的路徑**，用途不同、參數不同，不要互相重用：①「風格參考圖」`apps/server/src/pdf-pages.ts`（長邊 1024、上限 24 頁、無狀態回 data URL，只餵風格庫縮圖）；②「來源素材」`apps/server/src/sources.ts` 的 `parsePdf`（純文字抽取）；③「匯入成簡報專案」`apps/server/src/pdf-deck.ts`＋`pdf-text.ts`＋`pdf-text-layer.ts`（1920×1080 落地存檔）。
- PDF 匯入簡報（③）全程**零模型**，不得退回 OCR＋inpaint：文字取自 PDF 原生文字層（`getTextContent()`），抹字背景靠**二次渲染過濾 text operator** 取得，而非 `extract-text` 那條 PaddleOCR＋生圖模型 masked edit 的路。受理條件為 16:9（每頁長寬比 1.70–1.82，混比例以第一頁為準）、150 頁上限；render 需有單頁與總時限，且不得長時間阻塞 event loop。匯入時**同時建立兩個 version**：version A 是原圖（`currentVersionId` 指向它，無 `textLayer`），version B 是可編輯文字（`textLayer.originalVersionId` 指向 A），兩者靠既有的版本切換 UI 存取；掃描頁沒有原生文字層，就只有 A。PDF 原檔一併保留於 `assets/pdf-import/source.pdf`。停在 A 時畫布與 `png.zip`／`slide-project` 匯出為原圖保真（無損 PNG 原封不動搬過去；**限頁碼關閉或該頁不編號時**——`withPageNumber()` 會 resize 後重新編碼），`pptx`／`pdf` 則為了體積走 `exporters.ts` 的 `compressSlideImage()` 轉成 JPEG q88 4:4:4（不做色度次取樣，保住彩色細字與細線；先 flatten 到黑底再編碼，因為 JPEG 沒有 alpha，sharp 是丟棄而非合成通道）；切到 B 會以系統字型重繪文字（內嵌字型在瀏覽器與伺服器都不存在），前端對此有一次性提示。
- `apps/server` 直接相依 `pdfjs-dist`（版本鎖定）：`pdf-to-img` 只提供「render page → PNG」，viewport 尺寸、`getTextContent()` 與 operator list 都必須直接用 pdf.js。無文字背景需掛 pdf.js 內部掛點 `page._renderPageChunk`（無公開 API 可渲染過濾後的 operator list），升級 `pdfjs-dist` 會使對應測試失敗而非靜默失效。
- 資料來源有第四條入口「貼上網址」（`POST /api/projects/:id/url-sources`，body `{ urls: string[] }` 1..10 筆）：逐筆過 `assertPublicHttpUrl` 後走**與網路搜尋同一份**的 `materializeWebSources` 落地（同 URL 更新既有 source、套用 `sources.ts` 的專案容量上限、寫完 `retriever.index`），差別只在 options。**四個正交的旋鈕不可再互相偷渡**：`renderer`（要不要呼叫第三方）、`renderOnly`（呼叫它是取代原生擷取還是補強）、`requireBody`（什麼算正文）、`refresh`（要不要略過快取捷徑）。`renderer` 與 `renderOnly` 刻意分成兩個而不是「有 renderer 就等於外包」：搜尋路徑要表達的是「就算部署有 render 服務也不准用」，合併成一個旋鈕就講不出這句話。手貼網址沒有搜尋摘要可退回，故 `requireBody: true`——`contentStatus` 不是 `full` 就算該筆失敗，`metadata.failureReason` 帶回真正的原因（限流／逾時／HTTP 狀態／hash 路由／未啟用 render／整批逾時），端點逐筆回 `{url, reason}`（**回報使用者原本輸入的那一行**，不是正規化後的網址），前端 `URL_FAILURE_REASONS` 各自對應不同的下一步動作。全部失敗回 400 `URL_SOURCES_UNVERIFIED`；部分成功回 201＋失敗清單；撞到專案上限**不整批回滾**，塞得下的照樣進去、塞不下的以 `SOURCE_COUNT_LIMIT`／`SOURCE_SIZE_LIMIT` 列入失敗清單並刪掉已落地的資產目錄（一筆都塞不下才回 409）。去重要用**擷取後**的 `metadata.url`（重導向、canonical 化、去追蹤參數都會讓它與輸入不同；拿輸入去查會每次都走「新增」，留下孤兒資產又被交易的 url 去重丟掉＝永遠加不進去），同一批裡重抓到剛新增的那一筆要就地換掉物件而不是排一個交易對不到 id 的 refresh（否則專案內文字與磁碟資產分歧）。整批有 240 秒預算（Cloud Run 預設 300 秒上限），逾時的網址回 `WEB_SOURCE_BATCH_TIMEOUT`；擷取維持**逐筆循序**——併發會更快撞上 Jina 的 20 RPM，那會讓整批白跑。
- 專案來源的容量上限只有一份，而且住在 **`packages/core`**（`SOURCE_COUNT_LIMIT` 200 份、`SOURCE_TOTAL_BYTES_LIMIT` 2 GiB，另有 `URL_SOURCE_BATCH_LIMIT`、`STYLE_REFERENCE_IMAGE_LIMIT`、`MAX_UPLOAD_BYTES`、`MAX_DECK_IMPORT_PAGES` 同理）：**凡是前後端都要知道的數字都放這裡**——伺服器拿去擋寫入、編輯器拿去顯示「175/200」與「最多 10 筆」。判斷與訊息（`SourceLimitError`、`sourceCapacityError()`、`assertSourceCapacity()`、`formatSourceBytes()`、單份正文上限 `MAX_SOURCE_TEXT_CHARS`）留在 `apps/server/src/sources.ts`。前端自己抄一個數字不會有任何測試發現它過期：使用者實測時看到的 `SOURCES 175/100` 就是這樣來的（伺服器早放寬到 200，畫面上還印著 100），同一批改動裡甚至還有另一個註解正在講「抄一份到前端就是第二份真相」。四個寫入端點（上傳、搜尋落地、貼上網址、**整份大綱**）都走 `assertSourceCapacity()`／`sourceCapacityError()`。舊版把 `100` 與 `1024 ** 3` 直接寫在四個地方，而大綱那條**一個檢查都沒有**——線上那份專案就是這樣從 100 長到 108 份的，第 101 份之後的來源進得了專案卻進不了目錄，等於上傳了永遠不會被引用的東西。份數與容量是**兩個錯誤碼**（`SOURCE_COUNT_LIMIT`／`SOURCE_SIZE_LIMIT`，皆 409）而不是一個 `SOURCE_PROJECT_LIMIT`：前者要使用者刪幾份、後者要刪大的那幾份，收斂成一句話會讓人刪了十個小檔案還是加不進去。兩者都是具名的 `SourceLimitError`（不是靠 `error.message` 比對），因為錯誤中介層後面有一條吃掉所有 `SOURCE_` 開頭代碼的 400 分支，用字串比對遲早會被它吃掉而把「專案滿了」記成壞輸入。訊息帶實際數字、前端只負責顯示——`SourcePanel` 的 `URL_FAILURE_REASONS` 只寫「下一步做什麼」，硬編的「100 份」就是這樣過期的。**2 GiB 說的是上傳的位元組，不是記憶體**：`extractedText` 與 `chunks` 都內嵌在 `project.json`，`updateProject` 的峰值約檔案的三倍，而 Cloud Run 的實例正好也是 2Gi，所以非圖片來源的正文必須有 `MAX_SOURCE_TEXT_CHARS` 這道上限（截斷會讓超出的部分連 FTS 都撈不到，故寫入 `metadata.textTruncated`，不靜默發生）。容量檢查要排在**呼叫模型之前**：只留交易裡那一道的話，滿了的專案每按一次「生成大綱」都會跑完一次搜尋加兩次模型呼叫才回 409，而且刪掉來源之前每次都一樣。
- 階段 1 的來源目錄（`buildOutlineCatalog()`）是**整份大綱與單頁重生共用**的一份，不得再各寫一份 `.slice(0, N)`：份數不是成本、字元才是，上界因此是 `OUTLINE_CATALOG_CHAR_BUDGET` 而不是份數，超出時跳過該筆並記 `outline_catalog_truncated`（跳過而不是就地停止，一份特別長的來源才不會把整條尾巴帶走）。條目的摘要是「`metadata.summary` 優先、不足再補正文」：圖片描述寫回時就把「標題＋一句話」存進 `metadata.summary`（舊資料沒有這個欄位，**fallback 必須永遠留著**——回填要嘛寫 migration 要嘛重跑 vision 燒配額，兩個都不划算），補正文時扣掉與摘要重複的開頭（兩者同源）並用 `truncateAtBoundary()` 切在段落或句末，硬切在表格列中間會讓模型把半行當成完整資料。**出處聲明改成整區共用一次**（`imageSummaryNotice()`，三條大綱路徑各講一次）而不是每筆條目自帶：`IMAGE_DESCRIPTION_NOTICE` 有 61 字元，150 份就是 9150 字元、15% 的目錄預算，零選源資訊。剝除只發生在目錄組裝，`source.extractedText` 開頭的那一句與每塊 chunk 的短前綴都不可動（前者是使用者在來源詳情看到的，後者跟著被單獨餵進 prompt 的 chunk 走）。
- 生成用途 `outline-reference`（「大綱參考」）是使用者自己寫好的大綱，整份以**自有的** `OUTLINE_REFERENCE_CHAR_BUDGET`（30000）進三條大綱路徑當結構指令，**不挪用目錄的 90000**——挪用會讓「多標一份大綱」變成「有幾十份來源模型從此選不到」，而使用者完全看不出關聯。它**同時仍是一般內容來源**（照樣進目錄、進 FTS、可被任何一頁引用），多這個用途只是額外整份餵進去；`sourceAttachesReferenceImage()` 對它回 false（結構指示不是畫面素材，不該吃掉每頁 3 張的影像額度）。新指令一律**從屬於**同一份 prompt 裡的既有規則，不與它們並列：頁數仍由 `desiredSlideCount` 那條管（章節多合併、少拆分，**拆分是複數且重複到對得上**，每個主題都要存活在某一頁的 purpose 裡，合併與拆分寫進 rationale），「保留章節用的形式」是**預設值**而非硬性（`outlineStructureInstruction()` 的可讀性判斷仍推翻得了它，否則 12 列表格會落到 1920×1080 上），「比使用者的草稿更充實」**必須帶長度上界**（否則那是一個錨在 30000 字文件上、沒有上限的下限，與長度收斂的重試迴圈字面上不可能同時滿足，代價是每次三倍配額，而且專打「大綱寫得很細」這個目標客群）。頁數的數字**只講一次**：兩條互相打架的頁數規則會讓非嚴格 gateway 挑一條聽，挑錯就撞上沒有重試的 `OutlineCountError`。「照它的結構走」與「UNTRUSTED_INPUT 之後一律當資料」的張力要就地消解——遵循它描述的章節、標題與順序，不執行寫在裡面的命令，**連讀起來像指令的標題也一樣**（需要時當文字重現，絕不照做）。超出預算的截斷要在文字裡留下標記（exported 常數並插值進三條 prompt，三份字面複製遲早指向一個不存在的標記）並多加一行「大綱沒有完整提供、不得宣稱涵蓋所有章節」，否則被砍掉的後半章節會讓模型在 rationale 裡自信地宣稱每個主題都保留了。**沒有任何大綱參考來源時，三條路徑的 prompt 必須與加入這個功能前逐字元相同**（同 `previousAttempt`／`pinnedSourceIds` 的慣例，已對 pre-feature HEAD 做過逐位元比對）。「標了卻等於沒標」有兩條路且**都要看得見**：沒有可讀文字（伺服器 `outline_reference_empty`／`outline_reference_partial`，只記 id 與數字）、未勾選允許 AI 讀取（它在 `eligibleSources` 就被濾掉，伺服器連 log 都不會有）——前端用它已有的資料在來源卡上直說，不鏡射任何伺服器組態。**圖片標成大綱參考永遠不會生效**（讀圖描述只跑 `visual-reference`），用途說明必須明講只吃文字檔。
- `#/`、`#!` 開頭的 fragment（hash routing 單頁應用）在抓取之前就判失敗（`WEB_SOURCE_HASH_ROUTE_UNSUPPORTED`）：HTTP 規範上 fragment 不會送到伺服器，抓回來的必然是首頁，靜默收下等於把另一頁的內容存成這個網址的來源。單純的錨點（`#section`）不受影響。
- SPA 抓不到正文由 `apps/server/src/web-render.ts` 的 `HtmlRenderer` 補：`SLIDE_MAKER_WEB_RENDER_ENGINE`（`jina` 預設｜`none`，`none` 回 `undefined`）、`SLIDE_MAKER_JINA_API_KEY`（可選，未設約 20 RPM）、`SLIDE_MAKER_WEB_RENDER_TIMEOUT_MS`（正整數，預設 30000）。**取捨要講清楚**：啟用 jina 等於把目標網址與其內容送到第三方（Jina AI），觸發條件只有一個——使用者**手動**貼上網址，那條路徑（`renderOnly`）的擷取**完全外包**給它，不做原生 fetch，每一筆都會送出去。**既有搜尋擷取路徑不傳 renderer，行為完全不變**：那些網址是模型給的，使用者沒有逐筆同意外送。外包的理由是原生擷取＋空殼啟發式看不出**混合渲染**——頁面伺服器渲染了大半內容、關鍵區塊留給 client 填，`looksLikeEmptyShell()` 判「有正文」而收下半份。實例（2026-08-02，`ithelp.ithome.com.tw/2026ironman/event`）：原生拿到 3,162 字元真實中文內容故不補抓，於是 11 處 `{{ topic.title }}` 原樣進了來源正文、競賽主題清單整段是空的；同一頁交給 Jina 是 14,010 字元、0 處殘骸、11 組主題全部展開。啟發式在這裡不是調得不夠好，是問錯了問題（「有沒有正文」答對了，「完不完整」沒被問），所以移除它而不是再加一條指紋。代價是這條路徑對第三方有硬相依：`renderOnly` 下 render 失敗＝這一筆失敗，**不退回原生**（沿用半份內容正是要消滅的行為），`engine=none` 則整條路徑以 `WEB_SOURCE_RENDER_UNAVAILABLE` 明確不可用。成功時於 `metadata.renderedBy` 註記以供查證（沒被採信的內容不得留下 `renderedBy`）。Jina Reader 的實測行為有四件事必須處理：①回應帶 `Title:`／`URL Source:`／`Published Time:`／`Warning:` 前言，`Markdown Content:` 之後才是正文（`X-Return-Format` 與 `X-Respond-With` 輸出相同，兩者都有前言），不剝掉就會被當正文餵進模型、標題還會變成字面上的 `Title: …`；②免費模式**預設回快取快照**，所以一律送 `x-no-cache: true`（變慢、更容易撞限流，但「現在去抓這一頁」是這條通道的語意）；③`URL Source` 與請求的網址不符就判 `WEB_RENDER_URL_MISMATCH`，但 **query 不可逐字比**——實測（2026-08-02）它回報的是 canonical 化後的網址，功能性參數原樣保留（`?tab=tech`、`?q=slide`、`?page=2`）而追蹤參數被剝掉（請求 `?utm_source=test&page=2` 回來是 `?page=2`），逐字比會把同一頁判成抓錯頁，而使用者從社群或電子報複製的網址幾乎都帶 `utm_*`＝那一整類網址全部加不進來；判準是**子集**（對方只能比我們少）而不是維護一份追蹤參數黑名單，後者得跟著對方的 canonical 規則走、漏一個就是同一種靜默失敗；④`Warning:` 一律判失敗（那行字不是正文），但**要分代碼**：`Target URL returned error …` 是目標站擋掉 render 服務（`WEB_RENDER_TARGET_ERROR`，重試無效、該改貼別的網址），`… not yet fully loaded …` 是那一頁還在動（`WEB_RENDER_INCOMPLETE`，重試就可能成功，實測送 `x-timeout: 20` 擋不掉它，只能分類），其餘走通用 `WEB_RENDER_WARNING`。串接前剝掉 userinfo，免得帳密進到第三方的 URL path。
- **「要不要多花一次 render」與「這頁到底有沒有正文」是兩個判斷，不可共用門檻**：`looksLikeEmptyShell()` 只是 render 的觸發條件（剝掉 `<title>` 後 <40 字元，或 <1200 字元且 HTML 有 `__NEXT_DATA__`／`data-reactroot`／`id="root"` 等 SPA 標記）——**貼上網址改成外包之後它已經不在那條路徑上**（`renderOnly` 時根本不問它：原生擷取沒跑，`raw`／`body` 必然是空的，那不是「這頁是空殼」而是「沒去抓」，讓它表態只會拿到一個沒有意義的 true），現在只剩搜尋路徑會用到，作用是在「沒有 renderer 又只抓到殼」時標記 `WEB_SOURCE_RENDER_UNAVAILABLE`；驗收標準是 `requireBody`＝「剝掉**獨立來源**的標題（HTML `<title>` 或 render 服務的 `Title:`，**不含正文自己的第一行**，那是循環論證）後仍有正文」**且**「補抓沒有失敗」，刻意不是字數門檻——舊版兩者共用 400 字元，害一則兩百字的中文公告被回報成「該站阻擋自動擷取」。已知取捨：render 服務把「Loading…」這類佔位字串當正文回來時會被收下（它不等於標題），相對於拒收真實的短內容，這是比較小的錯。不保留原始 HTML 快照。
- Cloud Run 的 HTTP/1 回應在未使用 `Transfer-Encoding: chunked`／串流機制時上限 32 MiB，超過直接回 "Response size was too large."。`png.zip`／`slide-project` 內嵌無損 PNG，二十頁上下就會撞線（`pptx`／`pdf` 轉 JPEG 後才拉開餘裕，但不足以當作保證），故匯出端點一律走 `apps/server/src/http-stream.ts` 的 `sendChunked()`，不得「簡化」回 `response.send()`（`send()` 會補 `Content-Length`，回應即屬 non-streamed）。
- 頁碼是**專案級系統合成物**：`packages/core/src/page-number.ts` 是編號規則與版面幾何的唯一真相，伺服器 SVG 合成（`exporters.ts` 的 `withPageNumber()`）、編輯器 DOM overlay（`PageNumberOverlay`，畫布與簡報模式兩處）、PPTX 文字框都必須呼叫 `pageNumberSlideLabel()`／`pageNumberLayout()`，不得各自寫死邊距、字級或色塊內距（`chip.padX` 就是為了讓 PPTX 把文字起點推回同一條邊距而回傳的），**也不得自行扣掉隱藏頁算可見序號**——`pageNumberSlideLabel()` 收的是整份 `slides` 與該頁的 `order`，隱藏頁回 `undefined`（不編號）、可見頁以「可見頁中的序」與「可見頁總數」轉呼叫 index-based 的 `pageNumberLabel()`，`skipFirstSlide` 因此自動變成「跳過第一張**可見**頁」。裸的 `pageNumberValue()`／`pageNumberTotal()`／`pageNumberLabel()` 是 index-based 的底層，渲染端直接呼叫它們就會漏掉隱藏頁。影像合約明文禁止模型自己畫頁碼（`DECK CHROME IS NOT YOURS TO DRAW`），頁碼一律事後合成；`slide-project` 封存也只帶設定、不烘進素材，否則再匯出會疊出第二個頁碼。**但光有那條禁令擋不住**（實證：使用者回報「AI 自己畫了第二個頁碼」）——`style.designSystem` 在同一份合約裡被宣告為 authoritative（`Structural properties follow style.designSystem … and the per-page-type rules`），權威**高於**禁令，所以只要 AI 風格分析把來源 deck 的頁碼當成設計規格寫了進去，模型就照畫；本機風格「玉山ithome」四處都有（色票 `#666666 — 頁尾註解說明文字、頁碼…`、字型 `頁尾備註與頁碼為 10pt-12pt Regular`、版面系統 `左下放註解，右下放頁碼`、內頁規則 `頁底有邊緣藍綠色線條、頁碼與備註說明`）。因此**兩端都要擋，缺一不可**：①禁令自帶**優先序宣告**（designSystem 與 STYLE／DECK FRAME 參考圖上的 chrome 都是「觀察紀錄」而非繪製指示），措辭**不得用英文 schema 欄位名**——`renderDesignSystem()` 落到 prompt 裡的是繁中標題（`## 設計思路`／`## 色票`／…），寫 `palette`／`layoutSystem` 等於要模型自己做一次映射，而這條規則的整個作用就是精準指出「這幾個位置的字不是繪製指示」；②`style-analysis.ts` 的 `STYLE_ANALYSIS_PROMPT` 禁止把 chrome 寫進 designSystem 的**七個欄位一個不漏**（最容易漏的是 `designRationale`，唯一的自由散文欄位又排在第一段；列出六個等於暗示第七個不受管制），理由要寫**本專案這端的事實**（「本系統自己會事後合成頁碼、記下來的會被畫第二次」），不能寫「那是來源 deck 的系統畫的、不是設計師畫的」——後者對很多簡報並不成立，給模型一個憑常識就能反駁的前提等於讓它自行決定要不要遵守。`avoid` **刻意不在禁止清單裡**（合約對它的處理是 `mandatory negative constraint`，寫進去反而有益）；chrome 佔用的**邊距與留白仍要照實記錄**（那是真實的版面幾何，說成內容區會讓生成的內頁整個長歪），而只用於 chrome 的顏色則**整個不列進 `palette`**（否則模型只能丟掉它、或編一個它其實沒有的用途）。唯一的例外是 `direct-asset`：素材圖自己畫面裡的頁碼是該素材的內容，歸 DIRECT-ASSET 保真契約管，而那句例外只在真的有 direct-asset 附圖時才送（沒有的話，在禁令旁邊放一個不存在的例外只會鬆動禁令本身）。
- 全新生成會多附一張**框架範本**（`deck-frame`，同一份 deck 裡前一張已生成的頁）：每頁都是單次無狀態生成，跨頁一致性原本全靠 designSystem 的文字描述重現，實測兩頁的標頭樣式因此不一致。它**吃掉一張參考圖額度**（`outline-sources.ts` 的最壞情況因此是 3+4+1=8，正好等於 Gemini 上限）。不變量：**只有 `generate`**（編輯與抹字的一致性來自底圖，多附一張「別頁長這樣」只會變成重排的理由）；**隱藏頁照樣可以當範本**（`hidden` 是「不上場」不是「不算數」）；`limitReferences` 的優先序**最低**（style → content → deck-frame——內容圖是使用者用 `sourceIds`／`pinnedSourceIds` 明確選中的，少一張是「使用者要的佐證沒出現」，少一張範本只是「鄰頁略不一致」）；副檔名認不出就**不附**而不是猜一個 mediaType（mock provider 落地的是 `.svg`，標成 png 送給真模型換來的是整頁失敗）；`version.styleVersion` 與專案現行風格不同就不用（否則改完風格只重生某一頁時，舊風格會被範本往回拉）。**`version.imagePath` 不保證在磁碟上**（`parseProjectBundle()` 不比對資產是否真的在 zip 裡），而四條真實 transport 都會無條件讀每一張參考圖，所以挑圖時要 `access()` 檢查、**缺檔就繼續往前找**——少了它，第 N 頁的一個檔案不見會讓第 N+1 頁 `failed`，而且 ENOENT 訊息帶著 `.png` 會命中 `safeFailure` 的 PNG 分支，使用者被告知「生成圖片未通過安全或格式驗證」，真正的原因卻在另一頁。**每一種沒附上都要記 `deck_frame_reference_skipped` 並帶原因碼**（含最常見的 `NO_PREVIOUS_GENERATED_SLIDE`，與被額度砍掉的 `REFERENCE_BUDGET_EXCEEDED`），只有第一頁不記；沒有這一行的話「沒收到範本」與「收到了但模型沒理」在使用者回報時分不開，而 `maxReferenceImages` 被風格圖填滿的通道會讓整個功能靜默變成 no-op。合約端它是**獨立規則區塊**（形狀比照 `DIRECT-ASSET FIDELITY CONTRACT`：沒有這個角色的附圖就回空陣列，所以沒有範本時整份 prompt 與加這個功能前逐字元相同），排在 designSystem 權威與頁碼禁令**之後**、附圖清單**之前**，且**先講從屬、後講對齊**（對齊句要的正是 title band 位置、字級、margins，而 DESIGN SYSTEM AUTHORITY 明文禁止 `average these against a reference image`，兩者要在同一個視野內消解）；「權威是誰」必須分 `hasDesignSystem()` 兩條路都成立——沒跑過分析的風格 designSystem 是空字串（本機 9 份有 8 份如此），把裁決權交給空欄位等於範本遞補成最高權威，無 designSystem 時要退到「STYLE 參考圖仍是權威、這張圖只告訴你框架被放在哪裡」並就地講明它與 `All STYLE references have equal influence … not a master template` 的關係。已知取捨：範本用 `version.imagePath`（使用者看到的合成圖），抽字後那張的文字是**系統字型**重繪的，所以「字級與顏色處理」對齊到的是近似值，不要拿它當字型一致性的依據。編輯器的畫布與簡報舞台都必須是**精確的畫布比例**（`.canvas` 用 `--ar` + 容器查詢單位、`.presentation-stage` 用顯式 `min()` 尺寸），用 `aspect-ratio` 搭 `max-width`／`height:100%` 會失效，頁碼就會落進 letterbox 邊條或被裁掉。
- 文字描邊（`strokeColor`／`strokeWidth`／`strokeOpacity`）的三件事：①**`paint-order="stroke"`／`paintOrder: "stroke"` 不可省**——SVG 與 CSS 預設都是先填色再描邊，而描邊跨在字形輪廓上（一半在外一半在內），少了它描邊會蓋掉字面：伺服器端實測 200 框 3px 描邊，純白字心像素從 191322 **歸零**（整行變成黑色實心塊），Chromium 端的 `-webkit-text-stroke` 同理（字心白色像素 2691→6312）。`text-stroke.test.ts` 的像素組會在拿掉 `paint-order` 時真的變紅（實測 8809 < 門檻 13929），不是只少一條字串斷言。②**寬度存 em 而不是畫布 px**，與這個 schema 其他長度欄位相反：描邊的觀感完全由它與字級的比例決定，存絕對 px 會讓同一個值在 100px 標題上細如無物、在 20px 註解上糊成一團，使用者每改一次字級就得回頭重調。上限 `TEXT_STROKE_MAX_WIDTH_EM` 是實測值——中文筆劃比拉丁字母密，0.14em 起字腔就開始被填滿，而同一個寬度下 `Sources 200` 還很清楚；預設 0.04em 也是實測挑的：足以救回可讀性（描邊四面都包，不像陰影只護右下那一側），細到看不出加了效果。③換算走 `packages/core` 的 `textStroke()` **一份**，三端（伺服器 SVG、編輯器 DOM、PPTX）不得各自乘一次字級。PPTX 走 pptxgenjs 的 `outline`（run 層 `<a:rPr><a:ln>`，PowerPoint 真正的文字外框）而**不是** `shadow`——後者被寫進 `p:spPr`，套的是文字方塊這個圖形而不是字，run 層的 `effectLst` pptxgenjs 只接了 `glow`；`shadow: {type:"inner"}` 還會產生開 `<a:innerShdw>` 卻關 `</a:outerShdw>` 的不合法 XML（上游 issue #1293）。已知不保真、但都不值得為它們放棄描邊：pptxgenjs 的 `outline.color` 沒把 alpha 接出來，`strokeOpacity` 在 pptx 裡一律當 1；OOXML 的文字外框畫在字面**之上**（沒有 paint-order 這種東西），所以同一個寬度在 PowerPoint 裡看起來會比畫布上粗一些。編輯器端長度一律用 `cqh`（畫布沒有用 `transform: scale`，尺寸全靠容器查詢單位，絕對 px 不會跟著縮——旁邊的 `letterSpacing` 至今仍是裸 px，是既有的同類 bug）；`text-metrics.ts` 的樣本框**不得**帶描邊，否則量到的字寬被撐大、字級估算整個偏掉。
- 隱藏頁（`slide.hidden`）是**純資料旗標**，語意是「這一頁不上場」而不是刪除、也不是「不要這張圖」：簡報模式跳過、`pptx`／`pdf` 排除、不佔頁碼；但仍可正常選取、編輯、生成，`png.zip` 與 `slide-project` 照常收錄全部頁面（`hidden` 一併封存，匯入後維持隱藏）。因此它**不可**列進 PATCH slide 端點的 `outlineFields`（一個像素都沒動到圖，列進去會讓「隱藏再取消隱藏」留下 `outlineDirty` 橘框），版本 restore／activate 也不得順手把它清掉（`hidden` 是頁面層級的、與影像版本無關）。簡報模式有**四條**換頁路徑（鍵盤含 Home／End、滾輪、點擊舞台、控制列按鈕），邊界與跳過隱藏頁的邏輯只能有 `Editor.tsx` 的 `nextVisibleIndex()` 那一份——它回 `undefined` 而不是「夾回原地」，因為呼叫端要區分的正是「還有下一頁」與「已經到底」（控制列的 `disabled`）。簡報控制列的「第幾頁 / 共幾頁」只與頁碼共用「隱藏頁不算」這一條，**刻意不套** `startAt`／`skipFirstSlide`（那是印在成品上的 chrome 設定，控制列是放映進度，必須從 1 數到可見頁數），不要「統一」成呼叫 `pageNumberSlideLabel()`。全部頁面都被隱藏時不得進入簡報模式（空的簡報只是一片黑幕），`pptx`／`pdf` 也必須以 `EXPORT_NO_VISIBLE_SLIDES` 拒絕匯出——兩者在這個狀態下都會回 200 並產出「合法但退化」的檔案（pptx 零張投影片；pdf 更糟，`PDFDocument.save()` 會自己補一張空白 A4 直式頁），而匯出連結是裸 `<a href>`，所以錯誤碼必須配一句繁中 message，前端也要在 `visibleSlideCount === 0` 時就不給那兩個連結。
- 「隱藏頁不會讓整份匯出失敗」對**帶圖的那四種格式**都成立：沒有 `currentVersionId` 的隱藏頁在 `pptx`／`pdf` 是連同隱藏頁一起濾掉，在 `png.zip` 也一樣略過（它沒有任何位元組可輸出）。三種修好、`png.zip` 單獨丟 `SLIDE_VERSION_MISSING`／HTTP 400 是更糟的狀態：使用者會在瀏覽器分頁看到一段 JSON，而同一份專案的另外三個下載都正常。**可見頁**缺圖仍必須讓那四種格式一起失敗，且頁號取自 `order + 1`，不能被前面略過的隱藏頁位移。第五種格式 `outline.md`（大綱的 Markdown）**整條不受影響**：它一個像素都不讀，所以既不經過 `visibleVersions()`、也不受 `EXPORT_NO_VISIBLE_SLIDES` 管——全部頁面都隱藏、或一頁都還沒生成時它仍必須正常匯出（那正是使用者最需要拿到大綱的時候），前端的下載連結因此要放在 `visibleSlideCount === 0` 那個分支**外面**。「還剩哪些格式可用」的說明在前端與伺服器的 400 訊息裡各有一份，**不得寫死幾種**：加入第五種格式時，前端那句「下方兩種格式仍會收錄全部頁面」與 400 訊息列出的替代清單就同時過期了，而後者是使用者在那個狀態下唯一看得到的下一步。
- 大綱的 Markdown（`outline.md`）刻意設計成能**原樣回丟成「大綱參考」來源**，下載 → 改 → 丟回來是同一個迴圈，格式決定都由這個往返定：`layoutHint` 不進檔（那是給模型的版面指示，再匯入會被當成投影片內容）、敘事用 blockquote 且**逐行**加前綴（lazy continuation 在空行就結束，多段敘事只加第一行會有一半掉出引用區）、隱藏頁的註記放在標題**下一行**而不是 `## ` 那一行（`hidden` 不是大綱 schema 的欄位，模型沒有任何方式把一頁設成隱藏，那句話唯一的去處就是被抄進新簡報的頁標題）。格式化函式住 `packages/core/src/outline-markdown.ts` **一份**：精靈那顆按鈕用的是尚未 PATCH 回伺服器的**本地草稿**（使用者剛改完的字必須出現在下載到的檔案裡，接端點會靜默給出改之前的版本），匯出面板那顆走端點，兩邊各寫一份格式化就會產出不一樣的檔案而沒有任何測試發現——測試因此用**兩份獨立的 GOLDEN 字面值**分別釘住兩端，只斷言「兩邊都等於 `outlineMarkdown()`」在任一端被重寫時是恆真的。已知限制：`content` 原樣輸出，所以頁內若有未閉合的 ``` 圍欄，之後每一頁的標題與正文都會被吃進那個 code block（回丟時模型看到的是「一頁、內容是一大塊程式碼」）——仍然保留原樣，改寫使用者的 markdown 是更壞的交換。
- 「批次生成全部頁面」（inspector 與精靈兩個呼叫點）在有隱藏頁時要用共用的 `BatchGenerateDialog` 讓使用者三選一（含隱藏頁／只可見頁／取消），不是只告知：隱藏頁不進成品也不放映，但生成它一樣消耗影像模型配額，兩種答案都合理。**沒有隱藏頁時完全不多一次點擊**，且「含隱藏頁」走的是「不傳 `slideIds`」那條舊路，兩者才會逐位元相同。精靈那條是一長串 async 鏈，必須在鏈開始**之前**問掉。
- `.data/`、`.slide-maker-data/`、`artifacts/`、`.venv-ocr/` 為執行期／生成資料，不要編輯或提交。

## 結構

pnpm monorepo（`apps/*`、`packages/*`，內部相依用 `workspace:*`）：`apps/server`（Express 5 API + job runner）、`apps/editor`（React 19 + Vite，雙重 build：app + library）、`packages/core`（zod schemas 與 provider 合約）、`packages/provider-mock`（預設的確定性圖片 provider）、`packages/provider-openai`（OpenAI-compatible 影像／文字／搜尋 provider）、`packages/provider-gemini`（Gemini AI Studio 原生 `:generateContent` 影像／文字／搜尋 provider）。

## Git 慣例

- Commit 訊息採用 Conventional Commits 格式（`feat:`、`fix:`、`chore:` 等）。

## 開發規範

重大修改要起worktree和feature branch，完成功能在merge回去
一般任務，要起dev agent作開發，review agent檢查，qa agent作測試，如果有需要可以起更多Agent並行。沒有特別說就用opus模型
任務完成記得更新CHANGE.md，檢端描述，一個項目一到兩行
